{
  "name": "shaimaa-almously-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run build && node --test tests/site.test.mjs"
  },
  "devDependencies": {
    "vite": "8.0.13"
  }
}
